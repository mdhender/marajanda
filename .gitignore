*~
*.db
*.db-*
*.pdf
*.png
*.swp

.DS_Store
.idea/

# Environment files (see dotenv). The *.local files may hold secrets, so
# they are explicitly ignored; shared .env and .env.<env> files are committed.
.env.local
.env.*.local

# build files that shouldn't be committed
/marajanda
tmp/

# overmind's control socket for a running instance
.overmind.sock
